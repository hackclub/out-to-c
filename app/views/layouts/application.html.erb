<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Out To C" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Out To C">
    <meta name="mobile-web-app-capable" content="yes">
    <link rel="icon" type="image/png" href="favicon.png" />

    <link rel="preload" href="<%= asset_path("InknutAntiqua/InknutAntiqua-Regular.ttf") %>" as="font" type="font/woff2" crossorigin>
    <link rel="preload" href="<%= asset_path("InknutAntiqua/InknutAntiqua-Bold.ttf") %>" as="font" crossorigin="anonymous" />
    
    <meta property="description"
      content="An official Hack Club YSWS where you code C and find treasure chests with IRL loot! If you've ever wanted to learn low-level or to own a Blåhaj, you should totally join!!" />
    <meta property="og:site_name" content="Out to C" />
    <meta property="og:title" content="Out to C" />
    <meta property="og:description"
      content="An official Hack Club YSWS where you code C and find treasure chests with IRL loot! If you've ever wanted to learn low-level or to own a Blåhaj, you should totally join!!" />
    <meta property="og:image" content="https://out-to-c.hackclub.com/banner.png" />
    <meta property="og:type" content="article" />

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>

    <% if Rails.env.development? %>
      <script type="importmap">
        {
          "imports": {
            "three": "https://cdn.jsdelivr.net/npm/three@0.183.2/build/three.module.js",
            "three/addons/": "https://cdn.jsdelivr.net/npm/three@0.183.2/examples/jsm/"
          }
        }
      </script>
    <% end%>
  </head>

  <body>
    <div id="loading">
      <span class="loader"></span>
    </div>
    <div id="notice" <% if notice == nil %> style="display: none;"<% end %>><span><%= notice %></span><button onclick="this.parentElement.style.display = 'none';">X</button></div>
    <%= yield %>
    <script>
    let travelDistance = typeof (_travelDistance) == "number" && parseFloat(_travelDistance) || 0;
    </script>
    <% if Rails.env.development? %>
      <script type="module" src="<%= asset_path "main.js" %>"></script>
    <% else %>
      <script type="module" src="<%= asset_path "min.js" %>"></script>
    <% end %>
    <% if session[:user_id] != nil %>
      <script src="<%= asset_path "runtime.js" %>"></script>
    <% end %>
  </body>
</html>
