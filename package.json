{
  "dependencies": {
    "esbuild": "^0.28.1",
    "three": "^0.183.2"
  },
  "scripts": {
    "build": "npx esbuild --bundle app/assets/javascript/main.js --format=esm --minify > app/assets/javascript/min.js && bash -c 'bundle exec rake assets:precompile' "
  }
}